ここにアイテム画像を置いてください。ファイル名はアイテムIDと一致させる必要があります（例: `Seal_S1_I1.png`）。アイテムIDは `shared/*_data.lua` の `VulfayneCollectibles.Finalize` が `<CategoryId>_S<シート番号>_I<アイテム番号>` の形式で自動生成しています。

画像が用意できていないアイテムは、図鑑UI上で単に画像なし（背景色のみ）で表示されます。
