ここにアイテム画像を置いてください。ファイル名はアイテムIDと一致させる必要があります（例: `Antique_S1_I1.png`）。アイテムIDは `shared/data.lua` が `Antique_S<シート番号>_I<アイテム番号>` の形式で自動生成しています。

画像が用意できていないアイテムは、図鑑UI上で単に画像なし（背景色のみ）で表示されます。

**現在100枚とも自動生成のプレースホルダー画像です**（テーマカラーのバッジにアイテム名を載せただけの簡易画像）。本物のイラストが用意できたら、同じファイル名で上書きしてください。
